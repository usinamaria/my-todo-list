# Public Vite variables (safe to be included in client bundle)
VITE_API_BASE_URL=http://localhost:3001
VITE_APP_NAME=My%20Todo%20List
VITE_PUBLIC_STRIPE_KEY=pk_test_123456789

# VITE_TARGET is used by the development proxy only
VITE_TARGET=http://localhost:4000

# Server-side secrets (DO NOT prefix with VITE_, keep these in .env.local or environment)
# SENDGRID_API_KEY=your-sendgrid-secret
# DATABASE_URL=postgresql://user:password@host/db
