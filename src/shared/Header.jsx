/**
 * Header Component
 * Displays the application header with the main title.
 * Shared component used across multiple features.
 */

/**
 * Header - Application header component
 * @component
 * @returns {JSX.Element} Header with application title
 */
function Header() {
  return (
    <h1>Todo List</h1>
  );
}

export default Header;
